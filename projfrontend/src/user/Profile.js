import React from 'react';
import Base from "../core/Base"

const Profile = () => (
    <Base title={"Profile Page"} description={"You are in profile section"}>
        <h1>This is Profile Page</h1>
    </Base>
)

export default Profile;